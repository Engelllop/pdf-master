/** Chrome de selección sobre el PDF: tinta del sistema, no azul de marca. */
export const SEL = 'rgb(var(--fg))'
export const SEL_PAPER = 'rgb(var(--toolbar))'

/** Lo que el borrador se va a llevar entero: mismo rojo apagado que el resto de los
 * estados destructivos de la interfaz, no un rojo saturado que compita con el plano. */
export const BORRA = 'rgb(var(--danger))'
export const BORRA_RELLENO = 'rgb(var(--danger) / 0.14)'
